import { styled } from '@mui/material/styles';

export const AnswerDiv = styled('div')(() => ({
  maxWidth: '520px',
  width: '100%',
  display: 'flex',
  justifyContent: 'space-between',
  alignItems: 'start',
}));
